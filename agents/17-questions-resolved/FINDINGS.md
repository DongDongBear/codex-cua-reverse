# FINDINGS — QUESTIONS.md resolver (agents 01–07)

Local disk only. Status is **ANSWERED** (with evidence path), **UNANSWERED**, or **OUT OF SCOPE**.
Prefer JS/.d.ts/traces/raw websocket over speculation. Native Swift bodies that are only strings stay UNANSWERED.

Counts: **185** questions — ANSWERED **156**, UNANSWERED **26**, OUT OF SCOPE **3**.

## Cross-check (contradictions between agents)

| Topic | Agents | Resolution |
|---|---|---|
| Tab paste Ctrl+v on macOS | 01 Q10 (possible bug) vs AX/sky | **Not a bug.** browser-service maps `ctrl`→`ControlOrMeta`→`Meta` on darwin. |
| getBrowser emit(undefined) | 01 Q17 vs live websocket | Code does `write(undefined,'cua.state')`; live model-visible parts did **not** include a stray `undefined` chunk (host named_outputs). |
| Dual `js` servers | 04 Q1 / 06 Q2 vs traces | Both MCP servers run; these threads advertised only `mcp__cua_repl` / unprefixed `js` and used tinysky. |
| `cua.getApp` vs `sky.*` | 02 Q28 / 05 Q15 / 01 | Unified tinysky is live. `sky` is `cua.computer` / `import('@oai/sky')`, not auto-global. computer-use MCP off. |
| Screenshot scheme | 01 Q18 / 02 vs types 'data URL' | Types say data URL; plugin says `file://`; tinysky accepts both; traces delivered `input_image` bytes. |
| Window2 on Mac | 02 Q20 | Window2 is Windows-only (`target:'windows'`). |
| First-use dump size / Playwright | 05 Q19 vs 01 Q8 | Browser getBrowser ≈50k (core+Playwright). Computer getApp after reset ≈21k core + AX, no Playwright. |
| Tab.ax hidden vs Target methods | 01 Q7 / 03 Q13–16 | Docs hide `Tab.ax`; live override on because `BROWSER_USE_TINYSKY_ENABLED=1`; `tab.click` works (traces). |
| Confirmations disk vs requestMeta | 01 Q20 / 03 Q25 | This capture used on-disk computer-use confirmations in `cua.core`. Browser `confirmations` readNames still required before CDP/WebMCP. |
| XPC vs socket | 02 Q23 / 07 Q1 | JS=JSON-RPC socket IPC-5; Client/sky.node=XPC NativeBridge-1; AE ferries XPC bootstrap port. |
| `js_reset` vs turn_ended | 05 Q21 / 04 Q6–8 / 06 Q6 | Kernel reset ≠ tab cleanup. 0127 kept bindings across turns. Native PiP uses notify `turn-ended`, not MCP. |
| noWindowsAvailable | 05 Q11–12 / 07 | Coordinate clicks need a key window; AX menu indexes can still work. Error −10005. |
| Numbers.md | 02 Q14 | File ships; JS `window_result` skips prefix for `com.apple.iWork.Numbers`. |
| Auth plugin version | 07 Q14 | Bundled 1000968 vs installed 1000366 — upgrade path exists (installer/Sparkle), did not run. |

## Table

| agent | q# | question | status | answer | evidence |
|---|---:|---|---|---|---|
| 01-cua-tinysky | 1 | Two different cua objects — is package-main cua dead? | **ANSWERED** | Dead for ChatGPT.app’s model REPL. unified-computer-use banners import only @oai/cua/tinyskyAlt and Reflect.set(globalThis,'cua'). Package-main cua.js never assigns globalThis and has no getApp/getTab. Nothing in ChatGPT.app sets TINYSKY_ALT_INITIALIZE_DOCS. Codex computer-use skill uses import('@oai/sky'), not package-main cua. | `/Applications/ChatGPT.app/Contents/Resources/plugins/openai-bundled/plugins/unified-computer-use/resources/banner.js`; `.../@oai/cua/dist/lib/js/oai_js_cua/src/cua.js`; `.../tinysky_alt/globals.js` |
| 01-cua-tinysky | 2 | Who sets TINYSKY_ALT_INITIALIZE_DOCS and should ChatGPT models call initialize()? | **ANSWERED** | Nobody in this ChatGPT.app tree (grep hits only create_tinysky_alt.js). Default docsKey is core-cua-repl, which omits initialize. Banner already await setupCUA(). initialize() is setupCUA()+getState() and is redundant. Traces never called it; js-tool-description says first call getState/getBrowser/getApp/getTab/createBrowserTab. | `create_tinysky_alt.pretty.js` docsKey(); `tinysky-alt-core-cua-repl.md`; `unified-computer-use/resources/js-tool-description.md`; traces/all-js-calls.json |
| 01-cua-tinysky | 3 | Optional members: types vs runtime vs plugin | **ANSWERED** | Runtime omits members (no stubs) when SetupOptions.browser/computer is false. getState is always installed by create_tinysky_alt despite getState? on TinySkyAlt. Type consumers treating getState as optional are wrong after setupCUA; assuming getApp on banner-browser.js sessions throws. | `tinysky_alt/types.d.ts`; `create_tinysky_alt.pretty.js`; `banner-browser.js` / `banner-computer.js` |
| 01-cua-tinysky | 4 | getApp/listApps vs getState.apps platform split | **ANSWERED** | Intended in current JS: getApp/listApps throw unless sky.target==='mac'. get_state.js calls computer.list_apps() for mac AND windows, [] for linux. Windows getState().apps can populate while getApp throws. TinySky does not bind Window2. | `.../oai_js_cua/src/get_state.js`; `create_tinysky_alt.pretty.js` getApp/listApps |
| 01-cua-tinysky | 5 | browserId: instance id vs client-type alias? | **ANSWERED** | Both. Browsers.get matches type (iab/extension/cdp), Chromium family (chrome/edge/brave/opera/vivaldi), else exact opaque id. id wins over url. get('chrome') is first find(); preferred instance is env BROWSER_USE_PREFERRED_EXTENSION_INSTANCE_ID. Live session ids were numeric '1', not the literal 'iab'. getForUrl/getDefault are documented:false but are the no-arg fallback. | `browser-service.mjs` matcher ux/Eh/bh; `create_tinysky_alt.pretty.js` helper g; traces listTabs browserId:'1' |
| 01-cua-tinysky | 6 | Point type tuple vs {x,y} vs AXPoint | **ANSWERED** | Tab AXPoint at runtime is a 2-number tuple: Wg=d.tuple([d.number(),d.number()]), click target = number \| tuple. Docs Vec2=[x,y]. Passing sky {x,y} into tab.ax.click fails that schema. App Target click/scroll/drag only Array.isArray → x,y; else element_index (so {x,y} becomes element_index). Computer drag_handle/move stay object points. | `browser-service.mjs` Wg/fT; `tinysky-alt-core-cua-repl.md` Vec2; `create_tinysky_alt.pretty.js` Array.isArray; `sky/types/Point.d.ts` |
| 01-cua-tinysky | 7 | Is tab.ax enumerable or hidden by disabled-member proxy? | **ANSWERED** | undocumentedApiMembers=['Tab.ax'] is added only in documentation assembly (Vm.readBrowser), not setup’s returned disabledMemberIds. Live proxy hides members in disabledMemberIds ∪ unsupportedByDefault unless apiSupportOverrides['Tab.ax']===true. With BROWSER_USE_TINYSKY_ENABLED=1 (live), qY sets Tab.ax override and disables Tab.cua/dom_cua. decorateTab Object.assigns Target methods onto the proxied Tab; those names stay visible. tab.ax.get from user JS works when the override is on because get trap allows 'ax'. decorateTab closes over the proxy and uses t.ax. | `browser-client.mjs` Proxy get + decorateTab; `browser-service.mjs` qY/Yy/WB setup return; live env BROWSER_USE_TINYSKY_ENABLED=1 |
| 01-cua-tinysky | 8 | Playwright subset and backends | **ANSWERED** | Not npm playwright.Page. Client class Ke RPCs playwright_* ; service injects __codexPlaywrightInjected. api.json PlaywrightAPI is the subset (locator/getBy*/evaluate read-only/waitForEvent download\|filechooser, no networkidle, no locator.setInputFiles). Client also has goBack/goForward not in PlaywrightAPI. Live IAB used pt.playwright.locator.fill/press/evaluate. decorateTab does not add playwright; it is a Tab field. | `browser-desktop/docs/api.json` PlaywrightAPI; `browser-client.mjs` class Ke; traces task-1 calls 10–14 |
| 01-cua-tinysky | 9 | listTabs vs getTab claiming; does claimTab mint a new id? | **ANSWERED** | listTabs unions user.openTabs+tabs.list by id (controlled overwrites), does not claim. getTab matches id or providerTabId in tabs.list then tabs.get; else claimTab(userTab). IAB has no Browser.user. Live IAB: listTabs id:'1', providerTabId uuid; getTab('1') reused id '1' (no new agent id). Extension claim returns a normal Tab; code uses user tab’s id when already in controlled list. | `create_tinysky_alt.pretty.js` getTab/listTabs; `get_state.js` get_browser_tabs; traces/task-1-ant-design-form.json |
| 01-cua-tinysky | 10 | Does ax.pressKey('Ctrl+v') become Cmd+V on Mac? | **ANSWERED** | Yes. tinysky Tab.paste always pressKey('Ctrl+v') with no platform branch. Service splits chords, maps lowercase 'ctrl' → ControlOrMeta (o3['ctrl+v']=['ControlOrMeta','v']), then Ah(key,platform): ControlOrMeta → Meta iff platform==='darwin'. Not a paste bug on ChatGPT.app darwin. | `create_tinysky_alt.pretty.js` paste; `browser-service.mjs` Ah/Xl/o3/Ql/press_key→cua_keypress |
| 01-cua-tinysky | 11 | Does sky md paste interpret Markdown? | **UNANSWERED** | JS always forwards format (default 'text'). Tab md writes text/plain then Ctrl+v (source as plain). Native has ComputerUseIPCPasteFormat (string rawValue) and UTI net.daringfireball.markdown plus NSPasteboard; Swift renderer is compiled. Cannot confirm from JS whether format:'md' is rendered or inserted as source. | `sky/types/window/Paste.d.ts`; `targets/mac/paste.js`; SkyComputerUseService strings `net.daringfireball.markdown` / IPCPasteFormat |
| 01-cua-tinysky | 12 | createBrowserTab visibility/sessionName capability throws? | **ANSWERED** | sessionName: throws only if nameSession is not a function; client always has the method. documents.json includes session-naming only for type=extension. IAB first-use dump had visibility guidance, not session-naming. visible: tinysky does capabilities.get('visibility').set without existence check — missing capability throws. Live IAB advertised visibility (dump includes Browser Visibility Guidance). Plugin: IAB uses visible; Chrome/Edge uses sessionName. | `create_tinysky_alt.pretty.js` createBrowserTab; `documents.json`; live 0127 getBrowser dump headings |
| 01-cua-tinysky | 13 | Exact markdown of browser.documentation() in environment=codex-app | **ANSWERED** | Live getBrowser dump (50,690 chars): part cua.core = tinysky-alt-core-cua-repl.md + on-disk confirmations (21,246); part cua.browser = tinysky-alt-other-browser-apis.md + browser.documentation() starting '# Selected Browser' Name: Codex In-app Browser Type:iab ID:1, then Browser Safety, Visibility, IAB tab mentions, Tab Cleanup, Control Interruption, API Use, generated API reference including Playwright (no Tab.ax / no accessibility.md). Large overlap with tinysky docs. Confirmations excluded from browser pack in core-cua-repl mode. | `0127 websocket.jsonl` function_call_output call_L5p0JtR28cUwYwLPKIq1YonC; `documents.json`; `create_tinysky_alt.pretty.js` emitter |
| 01-cua-tinysky | 14 | Does environment codex-app change browsers/docs/disabled members? | **ANSWERED** | setup rejects anything except codex-app\|training\|cloud. Only cloud changes elicitationDisplayName to 'Cloud browser'. disabledMemberIds come from Gl(env) plus per-browser unsupportedByDefault/overrides. cdp/cloud enables requestManualHandoff when tab has cdp capability. tinysky does not pass environment (defaults codex-app). training is accepted but unused on this desktop (no evidence). | `browser-service.mjs` setup(); `browser-client.mjs` environment??'codex-app' |
| 01-cua-tinysky | 15 | Is globalThis.agent leftover or supported? | **ANSWERED** | Installed when browser enabled: Reflect.set(globalThis,'agent', raw Agent). Never in skill/js-tool-description. Package-main cua does not set it. Treat as undocumented escape hatch (agent.browsers / agent.documentation). Traces bound `browser` from cua.getBrowser, not globalThis.agent. | `create_tinysky_alt.pretty.js` Reflect.set; js-tool-description.md; traces |
| 01-cua-tinysky | 16 | BrowserInfo.name/type required vs optional; reconstruct @oai/browser types | **ANSWERED** | api.json list() requires name:string and type iab\|extension\|cdp. tinysky BrowserInfo and core-*.md make them optional. No @oai/browser package on disk; reconstruct from api.json + browser-client.mjs. Live documentation printed Name and Type as required fields. | `browser-desktop/docs/api.json`; `tinysky_alt/types.d.ts`; live Selected Browser block |
| 01-cua-tinysky | 17 | getBrowser emit of undefined — does nodeRepl.write drop it? | **ANSWERED** | Emitter does write(undefined,'cua.state') because JSON.stringify(undefined) is JS undefined and ''!==undefined. Kernel write() does not drop it: formatLog uses util.inspect → string 'undefined' as a named output. Live getBrowser result had 3 parts (wall-time, cua.core, cua.browser) and no standalone 'undefined' chunk — the MCP/host presentation of named_outputs did not surface that cua.state write as its own part. 'undefined' in the dump is TypeScript (`Promise<undefined \| Tab>`). | `create_tinysky_alt.pretty.js` emit; `_tmp/node_repl_js/worker-runtime.js` formatLog/write; 0127 call_L5p0JtR28cUwYwLPKIq1YonC output |
| 01-cua-tinysky | 18 | Screenshot URL scheme for apps | **ANSWERED** | Sky Screenshot.url typed as data URL. window_result passes through whatever string native returns (empty→null). tinysky v() accepts data: (base64) or fileURLToPath. Plugin/computer-use-node-repl.md: file:// PNG in this environment. Live getScreenshot was delivered as input_image (emitImage of PNG bytes), not a URL in the text channel. | `types/window/Screenshot.d.ts`; `window_result.js`; `create_tinysky_alt.pretty.js` function v; 0223 create#4 input_image |
| 01-cua-tinysky | 19 | disableDiffing (CUA) vs disableDiff (sky) | **ANSWERED** | TinySky StateOptions.disableDiffing maps to sky disableDiff and AX disableDiffing. Forwarding sky name disableDiff into getAXState is ignored (no disableDiffing). Confirmed in helper a()/d() in create_tinysky_alt.pretty.js. | `create_tinysky_alt.pretty.js` functions a,d; `types/window/GetAppState.d.ts` |
| 01-cua-tinysky | 20 | Confirmation policy source of truth in ChatGPT.app | **ANSWERED** | This capture shipped on-disk markdown: cua.core includes '# Computer/Browser Use Confirmation Policy' (21k combined with core-cua-repl). tinysky uses requestMeta['openai/confirmation_policies'].computer_use only if a non-empty string ≤12000 UTF-8 bytes; else tinysky-alt-confirmations.md. Browser service has the same override for browser_use when documentation.get('confirmations'). No evidence requestMeta overrode the disk file here. Plugin js-tool-description does not inline the policy. | `documentation.js` / create_tinysky_alt emitter; `browser-service.mjs` Vm.read confirmations; live 0127 cua.core |
| 01-cua-tinysky | 21 | listBrowsers vs getState().browsers | **ANSWERED** | listBrowsers → browsers.list() (no tabs). getState().browsers → each BrowserInfo plus tabs from get_browser_tabs. listTabs flattens and stamps browserId. Types: State.browsers is BrowserState[] = BrowserInfo & {tabs}. | `get_state.js`; `types.d.ts` State; `tinysky-alt-core-cua-repl.md` |
| 01-cua-tinysky | 22 | Tab.markDeliverable / markHandoff / requestManualHandoff on desktop | **ANSWERED** | markDeliverable/markHandoff documented on tinysky Tab; api.json unsupported on cdp. IAB first-use dump includes Tab Cleanup instructing both marks (agent-created IAB tabs close at turn end unless marked). requestManualHandoff unsupported on iab/extension/cdp by default; re-enabled on cdp when tab advertises cdp capability; not in tinysky Tab docs. Desktop ChatGPT.app is iab+extension — marks are the cleanup API, not cloud handoff. | `api.json`; `docs/tab-cleanup-iab.md`; live IAB dump Tab Cleanup section |
| 01-cua-tinysky | 23 | Package-main initialize vs tinysky setupBrowserRuntime options | **ANSWERED** | Confirmed: only tinysky decorateTab + excludedDocumentation confirmations when core-cua-repl + sets globalThis.cua and agent. Both pass undocumentedApiMembers Tab.ax and default environment codex-app. Importing @oai/cua leaves tabs with ax but no Target click/getAXState. | `cua.js` vs `create_tinysky_alt.pretty.js` |
| 01-cua-tinysky | 24 | Computer in node-repl docs is a lie about mac | **ANSWERED** | core-node-repl.md Computer only lists target + optional drag_handle/get_screenshot/move (linux full-desktop). ChatGPT.app darwin live cua.computer is WindowComputerUseClient. Default injected doc is core-cua-repl (no those methods). Models following node-repl on this app would fail cua.computer.get_screenshot(). | `tinysky-alt-core-node-repl.md`; `sky.d.ts` WindowComputerUseClient; docsKey default |
| 01-cua-tinysky | 25 | Tool name cua_repl vs node_repl | **ANSWERED** | MCP server name is cua_repl wrapping bin/node_repl. Model-facing tool is unprefixed js (plus js_reset). JS global is nodeRepl, not cua_repl. js-reset.md’s 'next cua_repl.js call' is docs alias for the next js invocation. | `unified-computer-use/.mcp.json`; intercept namespace mcp__cua_repl tools js/js_reset; traces name:'js' |
| 02-sky-native | 1 | Exact match order for app: string in Swift | **UNANSWERED** | JS only forwards the string (display name, bundle id, path, process name, list_apps id). Native resolution is compiled. Strings include CFBundleDisplayName, localizedName, bundleIdentifier, appPath. Exact order vs path vs process name is not in JS/.d.ts. | `types/window/GetAppState.d.ts`; SkyComputerUseService strings; `list_apps.js` |
| 02-sky-native | 2 | Spotlight predicate 14-day bound | **ANSWERED** | MCP/list_apps docstring: 'currently running, as well as any that have been used in the last 14 days'. Predicate string is truncated in the binary: kMDItemLastUsedDate_Ranking >= $time.today(- . Numeric 14 is not in the remaining C string; 14-day copy is the shipped contract. | SkyComputerUseService strings around kMDItemLastUsedDate_Ranking and 'used in the last 14 days' |
| 02-sky-native | 3 | Are running-but-never-used apps always in list_apps? | **ANSWERED** | Docstring union: currently running AND used in last 14 days. Native uses NSWorkspace.runningApplications / runningApplicationsWithBundleIdentifier plus Spotlight. Running apps are included even if Spotlight last-used is stale; 'No longer visible to NSWorkspace' is a related string. Not a Spotlight-only list. | list_apps docstring; NSWorkspace/runningApplications strings |
| 02-sky-native | 4 | Who needs native isFrontmost/appPath dropped by JS list_apps? | **ANSWERED** | JS list_apps maps id=bundleIdentifier??displayName??'unknown' and drops appPath/isFrontmost. Policy layer then rewrites input.app → target.appPath after getAppPolicy. Telemetry/response meta use bundleIdentifier. No other JS caller of isFrontmost found. Native still uses appPath for actions. | `list_apps.js`; `computer-use-policy.js` f() rewrite app→appPath |
| 02-sky-native | 5 | When policy rewrites app→appPath, is session keyed by path? | **UNANSWERED** | JS freezes rewritten path into subsequent RPCs; get_app_state return echoes the caller’s original app string (window_result app:e). Native session key (path vs bundle) is compiled. A later get_app_state({app:'Chrome'}) goes through policy again and would rewrite to the same path if resolution is stable. | `computer-use-policy.js`; `window_result.js` |
| 02-sky-native | 6 | Is startApp / ComputerUseIPCAppStartRequest only for MCP/old schema? | **ANSWERED** | Public JS sky.get_app_state never calls MacComputerUseClient.startApp. Launch is inside GetSkyshot ('Start an app use session if needed'). startApp RPC exists on the client and in the binary. MCP get_app_state docstring still says that once-per-turn sentence. Unused by tinysky/sky JS wrappers. | `targets/mac/client.js` ComputerUseIPCAppStartRequest; get_app_state.js; MCP docstring string |
| 02-sky-native | 7 | Launch hidden vs activateIgnoringOtherApps | **UNANSWERED** | Binary has activateIgnoringOtherApps:. Plugin says background/implicit launch. JS does not pass an activate flag. Actual NSWorkspaceOpenConfiguration (hidden vs activate) is compiled. | SkyComputerUseService `activateIgnoringOtherApps:`; computer-use-node-repl.md |
| 02-sky-native | 8 | Which process if two .app bundles share a name? | **UNANSWERED** | ambiguousApp (−10018) message is about shared bundle id: use name or full path. Name-collision behavior is unspecified in JS/strings. | errors.d.ts ambiguousApp; binary 'Multiple apps share this bundle identifier' |
| 02-sky-native | 9 | Screenshot pixels vs points; should_normalize_screenshot_to_point_resolution | **UNANSWERED** | Flag exists next to jpeg_compression_quality / chrome_extension_install_enabled (likely a Statsig/config key). JS does not send it. Plugin: X/Y in app-window screenshot. Pixel vs point not decided in JS. | SkyComputerUseService string should_normalize_screenshot_to_point_resolution; Click.d.ts |
| 02-sky-native | 10 | Exact settle timings | **UNANSWERED** | Plugin copy ~1s + up to 5s on loading indicators. Native fields needsUISettleBeforeSkyshot, lockUISettleDelay, lockUIFallbackDelay exist; numeric values not in JS or as plain strings. | computer-use-node-repl.md; native field names |
| 02-sky-native | 11 | When feature/axTreeDiffing is off, does disableDiff:false still return a full tree? | **UNANSWERED** | Flag feature/axTreeDiffing 'Enables AX tree difference rendering.' Default on/off for 26.902.1000968 is compiled/Statsig. JS always sends disableDiff through. Cannot see server-side gate from JS. | binary feature/axTreeDiffing |
| 02-sky-native | 12 | Diff marker dialect ~+- vs Removed element IDs | **ANSWERED** | Both strings ship. feature/axTreeDiffingRemovedElementIDRanges 'Summarizes removed AX tree elements as consolidated ID ranges'. Live 0127/0223 diffs used '~ and +' plus added-index lines (e.g. '+4594 text'), not a 'Removed element IDs:' footer in the captured trees. Default for this build’s live session is ~/+ (removed summarized or omitted), not the three-marker ~+− dialect. | binary both copy blocks; 0127 create#6 AX diff text |
| 02-sky-native | 13 | When cumulative vs previous diff? | **UNANSWERED** | Both sentences exist: 'diff from the previous accessibility tree' and 'cumulative diff from the initial accessibility tree'. Captured trees used the previous-diff wording. Switch condition is compiled. | native strings; live AX diffs |
| 02-sky-native | 14 | Why JS suppresses Numbers.md prefix but still ships Numbers.md | **ANSWERED** | window_result.js has Set(['com.apple.iWork.Numbers']) and skips <app_specific_instructions> for that bundle. The file still exists in Package_ComputerUse AppInstructions for other clients (MCP/native) that do not use this JS skip. JS-only quirk. | `targets/mac/window_result.js`; `Package_ComputerUse.bundle/.../AppInstructions/Numbers.md` |
| 02-sky-native | 15 | Does GetSkyshot always include a screenshot file when classifier says no useful image? | **UNANSWERED** | feature/skyshotClassifier 'determine if Skyshot contains image or not'. JS throws if native omits skyshot object, allows null/empty screenshot URL. Whether classifier drops the file vs returns empty URL is compiled. | `window_result.js`; feature/skyshotClassifier |
| 02-sky-native | 16 | Full forbidden/denied/high vs low tables | **UNANSWERED** | Only C-string samples: SecurityAgent, LocalAuthentication.UIAgent, WindowManager, controlcenter, notificationcenterui, UserNotificationCenter, Terminal, iTerm2, Warp, kitty, 1Password, bitwarden, dashlane, proton pass, Safari, SafariTechnologyPreview, firefox, finder, systempreferences, dock, plus ComputerUseAllowForbiddenTargets near finder. Classification is compiled Swift. Not a complete table. | SkyComputerUseService nearby bundle-id strings; computer-use-policy.js decision enum |
| 02-sky-native | 17 | allowPersistentApproval criteria | **UNANSWERED** | Native returns allowPersistentApproval boolean; JS uses persist session\|always if true else session only. MDM vs per-app risk logic is compiled (target.risk high\|low, warningSubtitle). | `computer-use-policy.js`; MacAppPolicyResult in FINDINGS |
| 02-sky-native | 18 | blockedURL (−10015) URL patterns | **UNANSWERED** | Types ComputerUseURLBlocklist / isURLBlocked / isWebBrowser. Applied in EventStream URL policy too. Google Docs-ish regex not recovered as a complete pattern from strings. Code −10015 blockedURL ends session. | `errors.d.ts`; native ComputerUseURLBlocklist* |
| 02-sky-native | 19 | Does org denied skip elicitation? | **ANSWERED** | Yes. JS switch: denied → throw organization policy; forbidden → throw safety; allowed → createElicitation. No elicitation on denied/forbidden. | `computer-use-policy.js` decision switch |
| 02-sky-native | 20 | Will window2 ever be a Mac target? | **ANSWERED** | No in this package. Window2ComputerUseClient target literal is 'windows'; create_client maps win32→window2, darwin→window (mac). Name is the second window API for Windows, not a second Mac API. | `types/window2/Window2ComputerUseClient.d.ts`; `load_options` / create_client |
| 02-sky-native | 21 | Window2 screenshotId cache lifetime | **UNANSWERED** | Windows-only; not live here. Types require cached screenshotId for coordinate clicks. Invalidation on resize/modal not in the copied JS. | `types/window2/Click.d.ts` |
| 02-sky-native | 22 | Window2 include_text default false vs Mac always-on AX | **ANSWERED** | Types: Window2 include_text default false, include_screenshot default true. Mac get_app_state always returns text. Intent not documented beyond token/UIA cost comments in FINDINGS; treat as by-design API split. | `types/window2/GetWindowState.d.ts`; `types/window/AppState.d.ts` |
| 02-sky-native | 23 | CodexComputerUseNativeBridge-1 vs CodexComputerUseIPC-5 | **ANSWERED** | IPC-5 = Unix JSON-RPC used by JS @oai/sky native-pipe. NativeBridge-1 = Electron sky.node (PIP cursor overlay, spawnComputerUseService, XPC to CUAService). SkyComputerUseClient MCP uses XPC ComputerUseIPCXPCTransport, still pings IPC-5 as API version string. | `native-pipe.js` CodexComputerUseIPC-5; `/Applications/ChatGPT.app/Contents/Resources/native/sky.node` CodexComputerUseNativeBridge-1 |
| 02-sky-native | 24 | Unused JS-visible RPCs: FrontmostWindow, AppStop, Messages, Calendar, Skysight | **ANSWERED** | Same SkyComputerUseService process. SkyComputerUseClient strings: separate MCP servers CalendarMCPServer (placeholder not_implemented), MessagesMCPServer, ComputerHistoryMCPServer, Record&Replay, plus Skysight IPC types. Not dead code in the service; not called by @oai/sky JS. Unified JS path does not exec that MCP. | SkyComputerUseClient MCP strings; native IPC type list; live computer-use MCP enabled=false |
| 02-sky-native | 25 | Is SKY_ENABLE_AUDIO set on ChatGPT.app cua_node? | **ANSWERED** | Audio methods omitted unless SKY_ENABLE_AUDIO=1. app.asar Da() requires SKY_ENABLE_AUDIO===1 && NODE_REPL_ENABLE_AUDIO===1 to pick *-audio skill variant. Live cua_repl child did not have them set (agent 04 env dump). Types keep audio optional. | `sky.js` audio gate; app.asar `function Da()`; live env |
| 02-sky-native | 26 | Relaxed vs normal Codex Computer Use.app — which is this tree? | **ANSWERED** | package.json publishConfig lists bin/mac/normal/ and bin/mac/relaxed/. This ChatGPT tree ships one nested app (no bin/mac/ directory extracted). Strings include ComputerUseAllowForbiddenTargets (relaxed-looking override) and no JS_RELAXED (JS_NORMAL is a JSValue enum, not the build flavor). Which variant ChatGPT copied is not labeled on the bundle; treat as the single shipped app, not proven 'normal'. | `@oai/sky/package.json` publishConfig; tree listing; ComputerUseAllowForbiddenTargets string |
| 02-sky-native | 27 | vendor/sky incomplete window2/full-desktop copies | **ANSWERED** | Incomplete extract. Real package in ChatGPT.app has the full union; agent 02 copied it under agents/02-sky-native/d.ts/. vendor/sky/types/SkyClient.d.ts still imports missing files. | `codex-cua-reverse/vendor/sky/types/SkyClient.d.ts`; `agents/02-sky-native/d.ts/` |
| 02-sky-native | 28 | Which plugin is enabled: cua.getApp vs sky.get_app_state? | **ANSWERED** | Live traces use cua.getApp — unified-computer-use / cua_repl. computer-use plugin is installed with bundledContentVariant node-repl (sky.* skill) but its MCP is enabled=false. unified .mcp.json ships enabled:false then Di() rewrites cache to enabled:true. Host does not ignore the rewritten cache flag. | traces; `~/.codex/config.toml` mcp_servers.computer-use enabled=false; Di() in app.asar |
| 02-sky-native | 29 | Which document does raw @oai/sky REPL inject? | **ANSWERED** | Raw @oai/sky does not inject macos/SKILL.md by itself. computer-use node-repl skill (copied over SKILL.md when computerUseSkillVariant==='node-repl') documents sky.* including paste. @oai/sky/docs/skills/oai_sky_lib/macos/SKILL.md is stale (omits paste, required scroll index). Unified tinysky path injects tinysky-alt-core-cua-repl.md instead, not the sky skill. | `vte()` app.asar; computer-use-node-repl.md vs docs/skills/oai_sky_lib/macos/SKILL.md |
| 02-sky-native | 30 | Linux get_screenshot return count / screen id | **OUT OF SCOPE** | Not live on this Mac. Types: Promise<Array<Screenshot>> {filepath, bytes, data_url} with no screen-id field. Per-monitor cardinality not in schema. | `types/full-desktop/GetScreenshot.d.ts` |
| 02-sky-native | 31 | Windows launch_app raw .exe how Window.app is populated | **OUT OF SCOPE** | Not live here. Skill/types: launch_app app id from list_apps or explicit .exe path; Window.app is AppIdentifier string that may be a full path. | `types/window2/LaunchApp.d.ts`; windows SKILL.md |
| 03-browser-desktop | 1 | After getBrowser({id:'iab'}), is browser.browserId literal iab or opaque? | **ANSWERED** | Matcher accepts alias 'iab' (type match) or exact id. Returned browser.browserId is the instance id. Live: Selected Browser ID: 1 and listTabs browserId:'1', not 'iab'. Later createBrowserTab/getTab should reuse browser.browserId ('1'), though createBrowserTab('iab', url) also works via alias resolution. | browser-service matcher; live dump ID:1; traces |
| 03-browser-desktop | 2 | Two Chrome profiles; how is preferred instance set? | **ANSWERED** | get('chrome') is find() first family match. Preferred instance: env BROWSER_USE_PREFERRED_EXTENSION_INSTANCE_ID (alias BROWSER_USE_PREFERRED_CHROME_EXTENSION_INSTANCE_ID) plus optional window id. ChatGPT.app must set that env; not observed on cua_repl child dump in agent 04 (only BROWSER_USE_AVAILABLE_BACKENDS=chrome,iab). | `browser-service.mjs` Pv/$U ox(); live env in 04 FINDINGS |
| 03-browser-desktop | 3 | Are brave/opera/vivaldi advertised to the model on this Mac? | **ANSWERED** | Service catalog includes them as extension families. Plugin/js description only names iab/chrome/edge. BROWSER_USE_AVAILABLE_BACKENDS live=chrome,iab. Diagnostics/chrome-troubleshooting can still mention family keys. Not advertised in the CUA js tool text. | browser-service family catalog; launch.mjs browser-description.md; live env |
| 03-browser-desktop | 4 | getForUrl when no tab already has that host | **ANSWERED** | Ranking: file:/localhost → prefer iab; else score open tabs (exact URL → origin+path → hostname → hierarchy); tie-break iab, preferred extension, any extension, first. If no tabs match, falls through that tie-break to iab/preferred/first — not 'user’s Chrome just because public https'. | agent 03 FINDINGS getForUrl dM/ZY; browser-service getForUrl |
| 03-browser-desktop | 5 | When does desktop ChatGPT.app list type:cdp? | **ANSWERED** | cdp is the GaaS/cloud backend (gaas-browser-environment / NODE_REPL_RUNTIME_MODE). Desktop live backends chrome,iab. requestManualHandoff documented as Cloud Browser and re-enabled only when type cdp + cdp capability. Not listed in this Mac session. | Dr.GaasBrowserEnvironment; live BROWSER_USE_AVAILABLE_BACKENDS |
| 03-browser-desktop | 6 | IAB providerTabId stability after markHandoff | **ANSWERED** | IAB has no claimTab. Live providerTabId was uuid e118c787-… alongside agent id '1'. Tab Cleanup: markHandoff keeps the tab across turns; docs say reuse this browser binding; empty lists after cleanup do not invalidate the browser. Whether the uuid stays forever across ChatGPT restarts is fail-closed on title/url drift (tab-mentions-iab). | traces listTabs; tab-mentions-iab.md; tab-cleanup-iab.md |
| 03-browser-desktop | 7 | getTabContext dead or gated? | **ANSWERED** | api.json documented:false and unsupportedByDefaultIn all three types. No live call. Treat as ungated-dead for model; may still exist on the client if not in disabled set from unsupportedByDefault (it would be disabled on all types unless override). | `docs/api.json` |
| 03-browser-desktop | 8 | Tabs.content unsupported on all types — dead? | **ANSWERED** | Yes on this desktop runtime: unsupportedByDefaultIn iab/extension/cdp with no override found. Dead for model. | `api.json` Tabs.content |
| 03-browser-desktop | 9 | After claimTab, does Chrome tab stay in the user’s strip? | **ANSWERED** | Docs: claiming does not move the tab into an agent tab group; it stays in the user’s strip. Visual badge besides nameSession not specified. IAB has no claim. | `docs/tab-claiming-chrome.md` |
| 03-browser-desktop | 10 | Does IAB nameSession no-op, throw, or name ChatGPT UI? | **UNANSWERED** | Client always has nameSession. session-naming.md is extension-only. IAB live dump omitted that section. tinysky would call it if sessionName option set (plugin tells IAB to use visible instead). Native IAB behavior (no-op vs UI title) not in JS. | documents.json when browserTypes:['extension']; live IAB dump |
| 03-browser-desktop | 11 | {visible:true} on Chrome/Edge — OS window or side panel? | **UNANSWERED** | Capability set(visible) RPCs browser_visibility_set to native host. Docs: 'visually presented to the user'. Whether that is OS window vs extension side panel is native-host, not in JS. IAB visible is the in-app browser pane. | `docs/capabilities/browser/visibility.md`; visibility.md |
| 03-browser-desktop | 12 | Viewport set vs OS resize vs CSS viewport | **ANSWERED** | Capability viewport set({width,height})/reset() is an explicit browser viewport override (CSS layout viewport), not 'resize the OS window to make screenshots prettier'. Docs forbid using it for screenshot size. | `docs/capabilities/browser/viewport.md` |
| 03-browser-desktop | 13 | Is Tab.ax override always present on desktop IAB/Chrome? | **ANSWERED** | qY: if BROWSER_USE_TINYSKY_ENABLED is set, o=(env==='1') for all types. Live env is 1, so override is on. If unset: iab uses Statsig gate codex-browser-use-tinysky enabledByDefault false; extension default on when In()!=='production' (In() returns 'production', so extension also needs the gate). This ChatGPT.app session is guaranteed by env, not by Statsig. | `browser-service.mjs` qY/Yy; live BROWSER_USE_TINYSKY_ENABLED=1 |
| 03-browser-desktop | 14 | playwright.goBack/goForward callable or stripped? | **ANSWERED** | Present on client Playwright class Ke. Not in api.json PlaywrightAPI, so generated docs omit them. Proxy disables only api-manifest members; goBack is not Tab.playwright.goBack in the manifest (methods on PlaywrightAPI). Likely callable on the live object. Navigation is documented as tab.back()/forward(). | `browser-client.mjs` class Ke goBack/goForward; api.json PlaywrightAPI |
| 03-browser-desktop | 15 | Any write-eval besides CDP send? | **ANSWERED** | playwright.evaluate is read-only (DOM is read-only error). Locator fill/type/click/press are writes via Playwright actions, not evaluate. CDP send is confirmation-gated and origin-scoped. No other write-eval API. | api.json evaluate docs; browser-service readonlyError; capabilities/tab/cdp.md |
| 03-browser-desktop | 16 | If Tab.ax override missing, does tinysky tab.click fail? | **ANSWERED** | Yes. decorateTab implements tab.click as t.ax.click on the proxy. If Tab.ax is disabled, proxy get('ax') returns undefined and click throws. On this desktop with TINYSKY env=1, override is set so it works (traces). | browser-client Proxy U(); create_tinysky_alt decorateTab; traces tab.click |
| 03-browser-desktop | 17 | Exact model-visible tool name | **ANSWERED** | Responses additional_tools namespace name mcp__cua_repl with nested tools js, js_reset. Trace events store name:'js'. Codex special-cases node_repl and cua_repl as unprefixed (app.asar Yg). Not mcp__cua_repl__js on this desktop host. | intercept payload in 03 FINDINGS; traces; app.asar Yg mention in 04 FINDINGS |
| 03-browser-desktop | 18 | js_reset 'next cua_repl.js call' leftover naming? | **ANSWERED** | Docs-only alias for the next js tool call on server cua_repl. Not a third MCP tool. Same as 01-25 / 04-2. | `js-reset.md`; .mcp.json enabled_tools |
| 03-browser-desktop | 19 | turn_ended model-hidden on purpose? | **ANSWERED** | In enabled_tools and plugin hooks; omitted from intercepted model namespace tools array. Hook-facing MCP tool, not advertised to the model in additional_tools. | unified plugin.json hooks; intercept tools array js/js_reset only |
| 03-browser-desktop | 20 | omit_tools_from code_mode — does code mode lose browser control? | **ANSWERED** | cua_repl tools omitted from code_mode/deferred. app.asar tne() attributes computer-use code-mode to mcpServerName node_repl. Code-mode JS is generic node_repl (no setupCUA banner; skill import @oai/sky / browser plugin service). Browser control is not removed; it is the other MCP server. | `.mcp.json` omit_tools_from; 04 FINDINGS tne(); browser SKILL mcp__node_repl__js |
| 03-browser-desktop | 21 | Why tinysky imports oai_js_browser client vs desktop service? Which documents.json? | **ANSWERED** | create_tinysky_alt imports vendored oai_js_browser/.../browser-client.js (byte-identical to desktop client). Trusted service is @oai/browser-desktop/service (handleRpc). Service reads documents.json next to itself (desktop package). Vendored oai_js_browser documents.json is older/smaller and unused by handleRpc. | `create_tinysky_alt.js` import; launch.mjs NODE_REPL_TRUSTED_SERVICES; 03 FINDINGS hash note |
| 03-browser-desktop | 22 | oai_js_browser browser-safety-training.md dead? | **ANSWERED** | Used when assembling docs for environment/training paths and older documentManifest. Desktop handleRpc uses desktop documents.json (has confirmations/session-naming/claiming). training environment is valid in setup() but not used on-device here. Treat oai_js_browser extra files as non-production for ChatGPT.app cua_repl. | `oai_js_browser` documents.json vs desktop docs/documents.json |
| 03-browser-desktop | 23 | chrome-troubleshooting.md scripts/chrome-is-running.js cwd? | **ANSWERED** | That markdown is in @oai/browser-desktop/docs and the chrome plugin skill. Scripts live in the chrome/browser plugin roots, not @oai/browser-desktop. Model is told 'plugin root' meaning the chrome@openai-bundled plugin directory (cache ~/.codex/plugins/cache/openai-bundled/chrome/…), not cua_node browser-desktop. | `docs/chrome-troubleshooting.md`; chrome plugin layout |
| 03-browser-desktop | 24 | Native-host pipe names / stale extension | **ANSWERED** | Pipes /tmp/codex-browser-use and \\.\pipe\codex-browser-use. Stale extension: chrome-troubleshooting says do not repair native host; tell user to reinstall Browser plugin. 'This browser requires agent request headers…' is a separate extension-update error string. Exact break mode if extension is stale: communication failure / header mismatch, not a silent fallback to AX-on-Chrome. | `chrome-troubleshooting.md`; browser-service header-policy strings |
| 03-browser-desktop | 25 | Does service still demand agent.documentation.get('confirmations') before tab_cdp_call when tinysky excluded it? | **ANSWERED** | assertRequiredDocumentationRead checks documentManifest requiredFor vs readNames. confirmations is requiredFor tab_cdp_call/tab_cdp_events/webmcp_*. Tinysky excludedDocumentation=['confirmations'] only strips it from first-use include list; it does not pre-mark readNames. So CDP/WebMCP still throws 'Required documentation has not been read: "confirmations"' unless the model (or tinysky) called documentation.get('confirmations'). Computer-use confirmations in cua.core do not satisfy the browser reader’s readNames. | `browser-service.mjs` assertRequiredDocumentationRead + Vm.readBrowser excludedDocumentation filter |
| 03-browser-desktop | 26 | Default ChatGPT.app CDP: allowlist vs full_cdp_access | **ANSWERED** | Env BROWSER_USE_FULL_CDP_ACCESS_ENABLED (Ul) exists. Default unset on live cua_repl child. CDP capability docs: origin-scoped permitted commands. Full CDP is an enterprise/env override, not default desktop. | `browser-service.mjs` Ul; capabilities/tab/cdp.md; live env |
| 03-browser-desktop | 27 | Did getBrowser({url}) reuse open IAB tab; is tab id '1' stable? | **ANSWERED** | Yes reuse: getBrowser does not navigate; listTabs showed the already-open ant.design tab with id '1' and providerTabId uuid; getTab('1') dumped that page’s AX. createBrowserTab would have called tabs.new() (duplicate). Agent id '1' was stable for the whole 0127 session (14 js calls, no reset). | traces task-1; create_tinysky_alt getBrowser vs createBrowserTab |
| 03-browser-desktop | 28 | Is browser handle from getBrowser the supported long-lived binding? | **ANSWERED** | Yes. Dump: 'Reuse this browser binding across later turns.' Model also used browser.tabs.get('1') in parallel with cua.getTab. Both refer to the same IAB tab. globalThis.agent is the same runtime but undocumented; the supported handle is the getBrowser return value / browser.browserId. | live Selected Browser block; traces `browser` and `pt` |
| 04-plugin-wiring | 1 | Dual js: hide generic node_repl, prefix, or last-writer-wins? | **ANSWERED** | Both servers run (PID 5746 node_repl, 5769 cua_repl). Traces are tinysky (cua.getBrowser) so they hit cua_repl. Tool name is unprefixed js because Codex special-cases server names node_repl and cua_repl (Yg). Not last-writer-wins on a single js: they are two MCP servers, but the model-visible additional_tools namespace in these captures was only mcp__cua_repl (js, js_reset) — generic node_repl tools were not in the advertised namespace for these threads. | 04 FINDINGS live PIDs; 0127 additional_tools mcp__cua_repl only; traces cua.* |
| 04-plugin-wiring | 2 | Is cua_repl.js a docs-only alias? | **ANSWERED** | Docs-only product name for the next js call. Intercepts/traces say js. Not a code-mode identifier in these captures (cua_repl omitted from code_mode). | `js-reset.md`; traces |
| 04-plugin-wiring | 3 | Is js_add_node_module_dir still on generic node_repl? | **ANSWERED** | Yes, stock node_repl implements four tools. cua_repl enabled_tools drops it. Generic mcp_servers.node_repl has no enabled_tools allowlist in config.toml so the model could see it there — but these CUA threads did not advertise that server’s tools. | node_repl binary tool list; cua_repl .mcp.json enabled_tools; config.toml |
| 04-plugin-wiring | 4 | Do use-case env strings append on the override path? | **ANSWERED** | Live cua_repl NODE_REPL_TOOL_OVERRIDES.js.description did not contain those sentences (agent 04). Overrides replace the whole description string. Generic node_repl has the three NODE_REPL_INSTRUCTIONS_USE_CASE_* cleared by Ei() when surfaces enabled. Append-on-override is not what the live override JSON shows. | PID 5769 NODE_REPL_TOOL_OVERRIDES; launch.mjs concatenation; Ei() |
| 04-plugin-wiring | 5 | Exact format of NODE_REPL_INSTRUCTIONS_USE_CASE_* | **ANSWERED** | Live filled values are single plain sentences (e.g. 'Control the in-app browser in conjunction with the Browser Plugin.'). Not markdown fences. Binary default js description is also prose. Exact parser beyond string append is in the rust binary; observed values are one-line sentences. | 04 FINDINGS env table; node_repl default js description string |
| 04-plugin-wiring | 6 | On Stop, are both cua_repl and node_repl turn_ended delivered? Double-close risk? | **ANSWERED** | Hooks: unified → cua_repl.turn_ended; browser/chrome plugins → node_repl.turn_ended. Both fire on the same Stop/Interrupt/SubagentStop if both plugins enabled (they are). Each trusted-service set is a different process (desktop service vs plugin browser-service.mjs). turn_ended is deduped per (session,turn) inside a given node_repl. Cross-server double-close of the same IAB tabs is possible in principle (two services); not proven from a live Stop capture. | plugin.json hooks; two live node_repl PIDs; turn_ended dedup string |
| 04-plugin-wiring | 7 | What do sky/browser services do with turn_ended? | **ANSWERED** | Envelope {hook_event_name, session_id, turn_id}; repeats ignored. Browser service detachTurn: clipboard cleanup + cdp.detachAllTabs if predicate. Tab cleanup docs: unmarked agent tabs close; claimed Chrome tabs released. Sky service: trusted_service_hooks; native also has CodexTurnEnded / notify argv SkyComputerUseClient turn-ended for PiP/lock-screen. Exact AX flush not in JS beyond those handlers. | browser-service detachTurn; node_repl turn_ended; config.toml notify |
| 04-plugin-wiring | 8 | Which turn-end channel is required for PiP / lock-screen guardian? | **ANSWERED** | Native PiP/lock-screen is driven by SkyComputerUseClient 'turn-ended' notify argv and in-process XPC, not by MCP turn_ended. MCP turn_ended is for trusted JS services (tab cleanup). browser.turn_ended / codex_turn_ended in Codex binary are connector-side names, separate. Guardian/lock-screen can also key off Darwin screen lock notifications. | config.toml notify=[SkyComputerUseClient, turn-ended]; sky.node RemoteHostedPIPContent; 07 lock-screen strings |
| 04-plugin-wiring | 9 | SubagentStop session_id=${agent_id} — do services treat agent_id as session key? | **UNANSWERED** | Hook interpolates agent_id into the session_id field. Services dedupe (session_id, turn_id). Whether they ignore mismatched parent session ids is not in JS beyond 'repeated notifications for the same session and turn are ignored.' | unified plugin.json SubagentStop; turn_ended schema |
| 04-plugin-wiring | 10 | Why is user mcp_servers.computer-use.enabled=false and no live SkyComputerUseClient MCP? | **ANSWERED** | User config.toml [mcp_servers.computer-use] enabled=false collides with the plugin server of the same name. Native UI still works via ChatGPT-launched SkyComputerUseService + @oai/sky/service. bundledContentVariant node-repl only swaps SKILL.md; it does not by itself disable MCP. Observed: user enabled=false wins. | `~/.codex/config.toml`; no SkyComputerUseClient process |
| 04-plugin-wiring | 11 | If native computer-use MCP were enabled, would the model see those tools plus js? | **ANSWERED** | Yes unless disabled_tools/enabled_tools say otherwise. Plugin .mcp.json has neither allowlist. Skill text forbids anything but node_repl for UI, but MCP tools would still be listed. Live MCP tools/list (unsigned) showed 10 sky-window tools (no paste). | computer-use .mcp.json; verify/results/mcp-ndjson.json; SKILL.md |
| 04-plugin-wiring | 12 | computerUseSkillVariant values besides node-repl and legacy-mcp; default if computerUseNodeRepl off | **ANSWERED** | app.asar vte(): if pluginName computer-use, n=e.computerUseSkillVariant; node-repl copies computer-use-node-repl.md; if variant !== 'legacy-mcp' and audio flags, suffix '-audio'; n??'default' for audio naming. null variant returns early (keep shipped SKILL.md confirmation-only). Feature computerUseNodeRepl is a desktop flag (n.Xl()). Observed this machine: bundledContentVariant/node-repl. Other enumerated value: live-disabled is for visualize plugin, not computer-use. | app.asar vte()/Da(); plugin.installed.json bundledContentVariant |
| 04-plugin-wiring | 13 | Is __codexInternalMarkBannerExecuted per-kernel so js_reset re-runs setupCUA? | **ANSWERED** | Yes. Kernel: jsBannerExecuted is a process local; js_reset kills the kernel ('js kernel reset') and the next js prepends the banner again. Live 0223: after reset, next getApp re-emitted 21,246 chars of Computer Use markdown (first-use flag reset). | `_tmp/node_repl_js/kernel.js` jsBannerExecuted; 0223 create#17/#18 |
| 04-plugin-wiring | 14 | Failure if user code also calls setupCUA? | **ANSWERED** | globals.js caches setup promise i; second setupCUA() reuses first SetupOptions and Object.assign is idempotent enough. Not live-eval confirmed, but the module-level singleton is in source. | `tinysky_alt/globals.js` |
| 04-plugin-wiring | 15 | Does launch.mjs keep NODE_REPL_JS_BANNER after kernel deletes it? Later spawn? | **ANSWERED** | Kernel deletes process.env.NODE_REPL_JS_BANNER in the child. launch.mjs parent never deletes it and spawns node_repl once (stdio inherit). No later spawn from the same wrapper. A js_reset respawn is inside node_repl (new kernel), which receives env from node_repl rust (sets TRUSTED_RPC_ENABLED=1 and re-supplies banner from its copy). Live post-reset banner ran. | kernel.js delete; launch.mjs spawn once; 0223 post-reset docs |
| 04-plugin-wiring | 16 | Can NODE_REPL_TOOL_OVERRIDES omit stock tools? | **ANSWERED** | Binary: 'omit' next to server_instructions/description/field_descriptions and 'cannot omit required field'. Omission of whole tools is via MCP enabled_tools (cua_repl drops js_add_node_module_dir). Override omit is for fields, not a third way to hide js. | node_repl strings; cua_repl enabled_tools |
| 04-plugin-wiring | 17 | Override merge vs replace nested maps; can title/timeout_ms field descriptions be overridden? | **ANSWERED** | Live JSON replaces js.description entirely (no leftover stock playwright blurb). launch.mjs only sets field_descriptions.code. timeout_ms remains in schema (traces advertised it, never set). Unknown whether rust merges nested field_descriptions; launch never sets title/timeout_ms descriptions so stock text remains unless omitted. | launch.mjs NODE_REPL_TOOL_OVERRIDES; traces js_parameters includes timeout_ms |
| 04-plugin-wiring | 18 | unknown node_repl tool override — allowed tool names? | **ANSWERED** | Stock tools: js, js_reset, js_add_node_module_dir, turn_ended. String 'unknown node_repl tool override: '. Those four are the allow-list implied by the binary. | node_repl binary strings |
| 04-plugin-wiring | 19 | code-mode JS is generic node_repl without setupCUA? | **ANSWERED** | Yes: omit_tools_from code_mode/deferred on cua_repl + tne() mcpServerName node_repl invocationSource code_mode. Skill path import('@oai/sky'). Outer agent uses tinysky cua_repl. | `.mcp.json`; app.asar tne(); computer-use-node-repl.md |
| 04-plugin-wiring | 20 | What is deferred as an omit target? | **UNANSWERED** | Named alongside code_mode in omit_tools_from. Binary/docs mention deferred tool world-state / tool_search. Exact executor not fully decompiled. Likely js then only reachable via tool-search in that mode. | `.mcp.json` omit_tools_from |
| 04-plugin-wiring | 21 | If Codex CLI starts without ChatGPT.app, does cua_repl stay disabled? | **ANSWERED** | Shipped and bundled-marketplaces copies stay enabled:false. Di() only rewrites plugin cache when desktop Zte() is true (browserUseTinysky, node paths, mcpToolExposure, plugin installed, not WSL). CLI without that rewrite leaves cua_repl disabled unless a previous GUI session left cache enabled:true (this machine’s cache is enabled:true). | Di()/Zte() app.asar; cache vs bundled-marketplaces .mcp.json |
| 04-plugin-wiring | 22 | Windows/WSL branch keeping command:node? | **ANSWERED** | Di() sets command=e.nodeRepl.env.NODE_REPL_NODE_PATH when nodeRepl!=null. Zte() is false when shouldUseWslPaths. Fi() has a win32 fallback. WSL path skips enabling cua_repl rather than keeping command:'node'. Shipped default command is 'node' until Di rewrites. | Zte() !o shouldUseWslPaths; Di() command assignment |
| 04-plugin-wiring | 23 | features.js_repl=false only gates first-party js_repl? | **ANSWERED** | Yes. config.toml js_repl=false; MCP node_repl and cua_repl still start. Binary distinguishes js_repl_node_path / js_repl_tools_only. Live processes prove MCP path is independent. | config.toml [features]; live PIDs 5746/5769 |
| 04-plugin-wiring | 24 | Is unprefixed js from non_prefixed_mcp_tool_names or hard-coded Yg? | **ANSWERED** | non_prefixed_mcp_tool_names string was not found in app.asar. 04 FINDINGS: Yg true iff name is node_repl or cua_repl. Unprefixed js is that hard-coded special case, not a general feature flag we could find. | 04 FINDINGS Yg; app.asar search non_prefixed_mcp_tool_names=-1 |
| 04-plugin-wiring | 25 | Does auto-review attach to unprefixed js or MCP server name? | **ANSWERED** | node_repl elicitation meta uses connector_id node_repl / tool_name js (binary strings). Guardian node_repl_policy treats node_repl and cua_repl as equivalent nested-tool sandboxes. Attachment is by MCP connector/server name plus tool js, not the unprefixed display name alone. | node_repl binary codex_approval_kind mcp_tool_call connector_id node_repl; 04 guardian policy note |
| 04-plugin-wiring | 26 | Is nested codex app-server always-on or only NODE_REPL_TRUSTED_RPC_ENABLED=1? | **ANSWERED** | Rust sets NODE_REPL_TRUSTED_RPC_ENABLED=1 when spawning kernel+worker (binary 'NODE_REPL_TRUSTED_RPC_ENABLED1'). Unset on the cua_repl wrapper env dump because rust injects it into the kernel, not necessarily the parent. Nested `codex app-server --listen stdio://` (PID 7250) is spawned by node_repl for approvals. Kernel only defines nodeRepl.rpc when that env is 1. Production desktop path is always-on for this MCP. | kernel.js rpc gate; 04 nested PID 7250; binary TRUSTED_RPC_ENABLED1 |
| 04-plugin-wiring | 27 | Is Group Container sock the production path when HOST_SERVICES_PIPE unset? | **ANSWERED** | Yes. native-pipe.js: SKY_CUA_SERVICE_NATIVE_PIPE_PATH else ~/Library/Group Containers/2DC432GLL2.com.openai.sky.CUAService/IPC/computeruse.sock. HOST_SERVICES_PIPE is fallback to ensureService. Live: sock exists, service child of ChatGPT.app, those two env vars unset on cua_repl child. Production macOS desktop path. | `native-pipe.js`; live sock; 04 env |
| 04-plugin-wiring | 28 | Who computes e.surfaces for Di()? | **ANSWERED** | Eo()/selection: `f&&d.length>0&&h.push('browser'), p&&h.push('computer')` then cuaReplSurfaces:h. f is browser-use enabled with backends d=browserBackends; p is computerUse. Desktop feature schema includes inAppBrowserUse, externalBrowserUse, computerUse, browserUseTinysky. Zte() additionally requires browserUseTinysky to even enable cua_repl. | app.asar cuaReplSurfaces construction @3293074; Zte(); feature schema |
| 04-plugin-wiring | 29 | User disables Computer Use but leaves Browser on — cua_repl browser-only? | **ANSWERED** | Yes. surfaces list omits 'computer' if p is false; launch.mjs then uses banner-browser.js, trusted services without sky, and computer-description stub 'Native computer APIs are disabled.' Inverse: banner-computer.js, no browser service. Di enable requires surfaces.length>0. | launch.mjs banner selection; app.asar surfaces push; Di r=nodeRepl&&surfaces.length>0 |
| 05-traces | 1 | When does the model use exec instead of js? | **ANSWERED** | These two tasks never invoked exec (0/0). exec is a custom functions-namespace orchestrator (lark grammar, tools.* inside a fresh isolate), advertised every create. CUA is mcp__cua_repl.js, outside exec by construction of additional_tools. Not proven that the model is forbidden to wrap js in exec; it simply did not. Plugin copy says use cua_repl for all UI actions. | calls.json exec_invocations:0; tinysky-alt-core-cua-repl.md; Appendix B |
| 05-traces | 2 | request_user_input vs request_user_input_async — is sync blocked? | **ANSWERED** | Both advertised every create. Only async used (one clarifying question). Sync is unused here, not shown as disabled. No desktop block flag found in local JS. | calls.json schema_tools_advertised; Appendix A |
| 05-traces | 3 | Handshake create with empty output (0127 #1) | **ANSWERED** | First uplink additional_tools + developer prompt, then response.completed with no message and no tools. Next create (#2) starts the Form task. Cache-warm / tool-schema inject, not a dropped user turn. 0223 create#1 already carries Form history plus Linear request (same thread). | 0127 websocket create#1 vs #2; 05 FINDINGS |
| 05-traces | 4 | js? in traces/*.json — join output_item.done? | **ANSWERED** | Yes. function_call_arguments.done has no name/call_id. The two js? rows are request_user_input_async and js_reset. Extractors should join output_item.done (and use its timestamp; 1ms skew on js_reset). | calls.json crosscheck_vs_traces disagreements |
| 05-traces | 5 | getBrowser({url}) vs createBrowserTab — would create duplicate? | **ANSWERED** | url on getBrowser is a selector (getForUrl), does not open/navigate. IAB already had that page. createBrowserTab calls tabs.new()+goto — would duplicate. Documented: selecting a browser does not open a tab. | create_tinysky_alt getBrowser vs createBrowserTab; traces call 1–3 |
| 05-traces | 6 | Live id '1' vs docs iab/chrome/edge | **ANSWERED** | Aliases are selector tokens for get/getBrowser({id})/createBrowserTab first arg. Runtime browserId/tab id were session-numeric '1'. Same as 03-1. | traces listTabs; live Selected Browser ID:1 |
| 05-traces | 7 | Two handles tab vs pt; does getTab omit playwright? | **ANSWERED** | Tab = BrowserTab & Target. decorateTab does not add playwright; it remains a field on the Tab class and is visible on the proxy (not a disabled api.json member named Tab.playwright — PlaywrightAPI is tab.playwright). First-use dump includes Other Browser APIs + generated Playwright types, telling the model locators live on the tab. Model still used browser.tabs.get for pt.playwright/dev.logs. cua.getTab’s wrapper does not omit playwright. | types Tab=BrowserTab&Target; other-browser-apis.md; traces call 6 vs 10 |
| 05-traces | 8 | Why Playwright after AX was working? | **ANSWERED** | Docs: use Playwright when indices are unstable or to batch. Indices did shift (325→4523→896…). Also DOM id= was easier for Validate Only. Intended trigger matches the dump; both motives apply. | other-browser-apis.md; traces calls 4–14 |
| 05-traces | 9 | tab.dev.logs vs AX for submit success? | **ANSWERED** | Call 6 wrote pt.dev.logs after basic form submit then clicked Fill form. AX after fill+submit already had a success-callback diff (call 5 result note). Console log was an extra signal, not the only one. | traces call 5–6; calls.json result notes |
| 05-traces | 10 | If IAB had been empty, documented path to open the page? | **ANSWERED** | Yes: getBrowser (or skip to createBrowserTab('iab'\|browserId, url, {visible})). Plugin: named browser → createBrowserTab directly; unknown → getBrowser({url}) then createBrowserTab(browser.browserId, url). User said the page was already open so the model skipped create/goto. | browser-description.md; js-tool-description.md |
| 05-traces | 11 | noWindowsAvailable on coordinate click while AX menu-bar clicks work | **ANSWERED** | Native errors: noWindowsAvailable alongside windowNotFoundForID / noFrontmostApp / cgWindowNotFound. Coordinate path needs a key-window screenshot; AX elementID clicks can still hit menu bar without that window. Raise is a documented secondary action; live Raise reported no AX change (already frontmost) while content window still missing for coordinates. Distinct from AX tree presence. | native strings noWindowsAvailable; traces click([119,35]) vs click(8) |
| 05-traces | 12 | pressKey('c') no-op — focus on menu bar vs web content? | **ANSWERED** | press_key is app-targeted, not global. Same session still on YOU-24844 Sandbox with no AX change. After Window menu 'New chat', index clicks worked on a different AX window ('New chat' / later HTML content). Keyboard went to whatever AX focused (menu/sandbox), not Linear’s composer. Matches docs: cannot fire global shortcuts; app-scoped. | 0223 dumps Window YOU-24844 Sandbox vs New chat; PressKey.d.ts |
| 05-traces | 13 | Why js_reset mid-task? | **ANSWERED** | Sits after Spotlight super+space (no AX change) and before let linear=await cua.getApp('com.linear'). Reset output 'js kernel reset'. First-use docs say after reset execute exactly one API call — next call did re-dump Computer Use markdown+AX. Likely model recovery after noWindowsAvailable, not a wedged REPL requirement. Docs do not require reset after errors. | 0223 create#16–18; js-reset.md; js-tool-description.md |
| 05-traces | 14 | Display name Linear vs bundle id com.linear — does bundle id change targeting? | **ANSWERED** | First getApp('Linear') succeeded (Sandbox AX). Policy rewrites to appPath; return text still says App: Linear. Rebind getApp('com.linear') produced the same Sandbox window dump (376 chars). Bundle id did not switch windows. Docs: retry bundle id when display name fails — here it had not failed (superstition / noWindowsAvailable recovery). | 0223 create#2 vs #10 dumps; list_apps id com.linear |
| 05-traces | 15 | Is sky.* still reachable from this js REPL? | **ANSWERED** | Not auto-global. cua.computer is the sky client when computer surface on. import('@oai/sky') still resolves from NODE_REPL_NODE_MODULE_DIRS. unified path does not strip the package. Traces never used sky.*. computer-use skill would assign globalThis.sky. | globals/create_tinysky_alt computer:y; 06 FINDINGS sky not auto-global; traces sky_invocations:0 |
| 05-traces | 16 | paste then setValue rewrite — wrong field or tightening copy? | **UNANSWERED** | Call 20: click(127) typeText title, click(128) paste body, click(131). Call 22 setValue(128, title) and setValue(130, shorter body). Indices 127 vs 128 vs 130 vs later 143 submit. No AX dump in this repo proving paste landed in the wrong node; equally consistent with the model tightening copy. Cannot decide from traces alone. | traces task-2 calls 20/22; 0223 AX diffs not fully stored in all-js-calls.json |
| 05-traces | 17 | How stable is assignee click index 257? | **UNANSWERED** | Single click in one New chat window. No second window to compare. AX indices are valid only for the tree that produced them (docs). Not a stable Linear assignee control id. | traces call 21; tinysky notes on indexes |
| 05-traces | 18 | Is 'New chat' Linear’s issue composer? Created-issue URL source? | **ANSWERED** | After Window menu, AX title became 'New chat' then fill/submit. Final assistant text cites YOU-25626 URL; no nodeRepl.write of that URL in js results (closing model text). Whether New chat is Agent/chat vs issue composer is product UX; the submit path did produce an assigned issue according to the assistant close. Not independently verified against Linear’s API in this repo. | 0223 create#21–27 dumps; 05 FINDINGS close text |
| 05-traces | 19 | First-use dump identical for browser vs computer? Does CUA_REPL_ENABLED_SURFACES slice it? | **ANSWERED** | Emitter always writes core-cua-repl (+confirmations) once per kernel. Browser extra (other-browser-apis + browser.documentation()) only when emit is passed a {browser}. Live: getBrowser → 21k core + 29k browser (Playwright types, LogEntry includes 'error'). Post-js_reset getApp → 21k core + 376 AX, no Playwright block. Surfaces flag does not slice core docs; it only chooses banner/providers. Computer-only sessions never get the browser pack unless a browser handle is emitted. | create_tinysky_alt emitter; 0127 getBrowser vs 0223 create#18 |
| 05-traces | 20 | {emit:false} only on browser getAXState? | **ANSWERED** | Used when the model then nodeRepl.write(filtered subset) in task 1. Native calls relied on Target auto-emit (diffs smaller, and no regex filter). Not a platform requirement — model choice. Default emit=true. | traces task-1 calls 8–14; ObservationOptions |
| 05-traces | 21 | Implicit reset on turn boundaries / turn_ended? | **ANSWERED** | 0127: 14 js calls, no js_reset, bindings (browser, tab, pt) survived the whole task (multiple response.create turns). turn_ended cleans tabs per cleanup policy, not the JS kernel. Only js_reset / kernel kill / sandbox change reset bindings. | 0127 no reset; kernel persistence @prev; js-reset.md |
| 05-traces | 22 | Did ambient IAB context bias the Linear task? | **ANSWERED** | 0223 create#1 still carries the 50k Form-task getBrowser dump in history plus IAB ambient note. Model went to cua.getApp('Linear') immediately and never touched tab.* in task 2. Ambient IAB was ignored for control after getApp. | 0223 create#1/#2; traces task-2 API set |
| 05-traces | 23 | 1 ms timestamp skew on js_reset | **ANSWERED** | traces used arguments.done 10:05:42.847Z vs output_item.done 10:05:42.848Z. Harmless. Prefer output_item.done (has name/call_id). | calls.json disagreement #2 |
| 05-traces | 24 | all-js-calls.json count 39 includes two js? rows | **ANSWERED** | Yes. Filter name=='js' && args.code for CUA js count (37). The two extras are request_user_input_async and js_reset. | `traces/all-js-calls.json`; calls.json |
| 06-repl-runtime | 1 | When does Electron flip cua_repl.enabled to true? | **ANSWERED** | Di() when Zte() (features.browserUseTinysky && nodePath && nodeReplPath && mcpToolExposure && plugin installed+enabled+AVAILABLE && not WSL) and e.nodeRepl!=null && e.surfaces.length>0. Not per-thread. BROWSER_USE_TINYSKY_ENABLED is the env analog; the feature flag is browserUseTinysky. Bundled-marketplaces copy stays false. | app.asar Di()/Zte(); live cache enabled:true |
| 06-repl-runtime | 2 | Which MCP server served the captured js traces? | **ANSWERED** | cua_repl (tinysky banner, cua.getBrowser). Wire name js. Generic node_repl also running but not in additional_tools for these threads. | traces cua.*; 0127 namespace mcp__cua_repl |
| 06-repl-runtime | 3 | NODE_REPL_TOOL_OVERRIDES merge; is timeout_ms still in schema? | **ANSWERED** | Override replaces js.description entirely. Schema still has timeout_ms (advertised, unused). launch.mjs only overrides field_descriptions.code. | traces js_parameters; launch.mjs |
| 06-repl-runtime | 4 | js_add_node_module_dir visibility / internal host use | **ANSWERED** | Not on cua_repl enabled_tools (model cannot call it there). Still implemented in node_repl; host could call it internally (no evidence it did). Directory added survives js_reset. | enabled_tools; kernel/js_add_node_module_dir strings |
| 06-repl-runtime | 5 | Working directory / session-id ChatGPT passes | **ANSWERED** | Live kernel argv: --session-id 93d6f09f52d74d719de7d5d1f9d5a6ca --working-dir /Users/dongdong/Documents/ChatGPT/Desktop then chdir. Workspace (ChatGPT Desktop folder), not CODEX_HOME or tmp. session-id salts internal binding names. | ps kernel.js cwd; kernel.js parseKernelBootstrapArgs |
| 06-repl-runtime | 6 | Does js_reset restart trusted worker or only untrusted kernel? | **ANSWERED** | Kills the Node kernel (untrusted VM). Bindings die; banner re-runs. Docs: does not close browser tabs or native apps. Trusted worker handlers Map would drop only if the worker process dies — reset path is 'js kernel reset' / kill kernel, not the trusted worker. Sky/browser sessions survive (post-reset getApp same Linear window). | js-reset.md; 0223 create#17–18 same Sandbox AX; kernel reset strings |
| 06-repl-runtime | 7 | Exec timeout vs withSuspendedTimeout; what happens on timeout? | **ANSWERED** | Default 30s. Trusted withSuspendedTimeout exists; untrusted bridge has no such method. Timeout: 'js execution timed out; kernel reset, rerun your request' (kills kernel). | node_repl strings; worker-runtime privileged withSuspendedTimeout |
| 06-repl-runtime | 8 | named_outputs presentation cua.core vs cua.state | **ANSWERED** | Kernel groups write(itemId) into named_outputs values-only Map order; unlabeled writes go to output. Host showed them as extra input_text parts after 'Wall time… Output:'. Live: cua.core then cua.browser (getBrowser) or cua.core then cua.state AX (getApp after reset). Item ids are not preserved in the model-visible text (values only). | kernel renderOutputEvents; 0127/0223 function_call_output parts |
| 06-repl-runtime | 9 | Who consumes exec_redacted_source? | **ANSWERED** | Kernel sends exec_redacted_source (identifiers→idN, strings wiped). Binary ties NODE_REPL_DISABLE_STRICT_AUTO_REVIEW / FORCE / ENFORCE_MODEL_CHECK and 'JavaScript execution requires an approval elicitation' / guardian auto-review. Telemetry/reviewer path; not shown to the model as js output. | kernel.js; node_repl strings exec_redacted_source / strict_auto_review |
| 06-repl-runtime | 10 | globalThis.agent leftover? | **ANSWERED** | Same as 01-15: undocumented escape hatch when browser enabled. | create_tinysky_alt.js Reflect.set |
| 06-repl-runtime | 11 | tools global — only js_repl/code_mode? | **ANSWERED** | Absent in this kernel (ReferenceError). features.js_repl=false; cua_repl omitted from code_mode. tools.* is the exec/code-mode orchestrator namespace, not this VM. | kernel createRuntimeContext; traces never used tools.*; exec unused |
| 06-repl-runtime | 12 | Does any production prompt still require initialize()? | **ANSWERED** | Not ChatGPT.app unified path (core-cua-repl + js-tool-description). core-node-repl.md still does if TINYSKY_ALT_INITIALIZE_DOCS=core-node-repl (unset here). Traces did not call it. | docsKey default; traces |
| 06-repl-runtime | 13 | Does anything still import @oai/cua package main? | **ANSWERED** | ChatGPT.app plugin banners do not. package.json main still exports cua.js. No other in-app importer found (grep). Treat as unused in this product path (may exist in Codex CLI/tests). | banner*.js; cua package.json exports |
| 06-repl-runtime | 14 | @oai/cdp-browser-backend 0.4.2 in package-map but not extracted — dead? | **ANSWERED** | Listed in package-map; no directory under lib/node_modules/@oai/. Dead reference in this archive. CDP desktop path is inside browser-desktop/service, not that package. | cua_node package-map vs node_modules listing |
| 06-repl-runtime | 15 | Is environment=training used on-device? | **ANSWERED** | setup() accepts training\|codex-app\|cloud. tinysky never passes it (codex-app). No live env selects training. On-device unused. | browser-service setup(); create_tinysky_alt setupBrowserRuntime args |
| 06-repl-runtime | 16 | How is NODE_REPL_TRUSTED_CODE_PATHS cache invalidated vs app updates? | **UNANSWERED** | Electron joins CODEX_HOME + cua_node node_modules. Plugin scripts under ~/.codex/plugins/cache/openai-bundled/…/26.903.61454/ are importable. Invalidation vs app update 26.903.61454 not in JS (cache dir is versioned by plugin version). No extra invalidator found. | trusted-worker resolve; plugin cache path |
| 06-repl-runtime | 17 | Who starts SkyComputerUseService? | **ANSWERED** | Live pid 3802 ppid 3579 ChatGPT.app. JS native-pipe launches via launchServices.openApplication (SKY_CUA_SERVICE_PATH / CODEX_HOME copy / bundle id) if sock missing; HOST_SERVICES ensureService if pipe set. notify turn-ended is separate. Production: Electron starts the service; JS can start it as fallback. | ps ppid; native-pipe.js; sky.node spawnComputerUseService |
| 06-repl-runtime | 18 | --disable-sandbox; is kernel landlocked via codex on Mac? | **ANSWERED** | Help: start kernel directly even when CODEX_CLI_PATH is set. Desktop sets CODEX_CLI_PATH to Resources/codex. Live kernel is `bin/node --experimental-vm-modules /var/folders/…/kernel.js` (tmp copy), spawned by node_repl, not a visible sandbox-exec wrapper in ps. Sandbox is primarily the rust/codex path when CODEX_CLI_PATH is set; Mac desktop still uses that helper unless --disable-sandbox. Exact sandbox-exec argv not in this ps snapshot. | node_repl --help; live kernel command; CODEX_CLI_PATH env |
| 06-repl-runtime | 19 | Sentry/Statsig events per js call | **ANSWERED** | Binary: codex/nodeReplExecutionDurationMs; sky telemetry CodexComputerUseMcpToolCalled (pluginId computer-use@openai-bundled, transport native_pipe). Client MCP: computer_use_mcp_server_launched / tool_called. NODE_REPL_DISABLE_ANALYTICS / BROWSER_USE_DISABLE_AMBIENT_NETWORK gate. Exact per-js Sentry payload not dumped (DSN present, not reproduced). | node_repl strings; computer-use-telemetry.js; 07 telemetry strings |
| 06-repl-runtime | 20 | Which models are gated by node_repl is unavailable for this model? | **UNANSWERED** | String exists with NODE_REPL_ENFORCE_MODEL_CHECK. Feature nodeReplEnforceModelCheck in desktop schema. Allow-list of model ids not in JS/strings recovered here. These traces used gpt-6-astra successfully. | node_repl string; app.asar nodeReplEnforceModelCheck; traces model |
| 07-native-app | 1 | When is XPC vs JSON-RPC socket vs Apple Events? | **ANSWERED** | JS @oai/sky: Unix JSON-RPC socket (IPC-5), never XPC. SkyComputerUseClient and ChatGPT sky.node: XPC (ComputerUseIPCXPCTransport / NativeBridge-1). Apple Event strings are the XPC bootstrap path (mach port + PID + request type from AE) with errors −10001/−10002/−10017/−10019 — leftover/companion to XPC, not the JS click path. AE is live for host bootstrap, not for tinysky actions. | `native-pipe.js`; sky.node RemoteHostedPIPContent; AE error strings |
| 07-native-app | 2 | What is SAIMachBootstrapRendezvous? | **ANSWERED** | Guardian spawn: register/look up Guardian Mach bootstrap rendezvous port, then 'XPC pipe from mach port' → NSXPCListenerEndpoint. Strings: Could not create/register/find rendezvous port; Could not receive XPC endpoint over XPC pipe. It is a Mach bootstrap rendezvous + xpc_pipe to vend an NSXPCListenerEndpoint, not a documented public SAI API we have source for. Not bootstrap_register-only in isolation — the type name is SAIMachBootstrapRendezvous. | SkyComputerUseService/Client strings SAIMachBootstrapRendezvous / NSXPCListenerEndpoint |
| 07-native-app | 3 | Socket peer auth: does same-uid random process fail -10000? Which check first? | **ANSWERED** | Live: unsigned Node and even OpenAI-signed cua_node/bin/node using raw net.createConnection were connected then kicked (socket closed) — not a successful ping. Trusted path is nodeRepl.nativePipe or signed SkyComputerUseClient XPC. ComputerUseIPCSenderAuthorization checks parent/responsible team+signing ids. Socket owner/path checks exist (unexpected owner, unsafe dir perms). Independent same-uid opener does not get a working session. Ordering of -10000 vs owner-path errors not sequenced in JS; live raw connect was close-without-RPC rather than a JSON -10000 (MCP list_apps from unsigned parent did return -10000). | LIVE-VERIFICATION.md; verify/results/native-ipc.json; errors.d.ts -10000 |
| 07-native-app | 4 | Does JSON-RPC ever send SCM_RIGHTS / file handles? | **ANSWERED** | JS native-pipe frames are uint32le+UTF-8 JSON only (max 8MiB); no SCM_RIGHTS in native-pipe.js. XPC sendRequest(...fileHandles:) and types ComputerUseIPCRequestWithFileHandles are XPC-only. Screenshots on the JS path are URLs (file:// or data:) in JSON, then JS reads the file. | `native-pipe.js` encode/decode; SkyComputerUseClient sendRequest fileHandles: |
| 07-native-app | 5 | com.openai.sky.computer-use-json-rpc-socket-readiness — queue or Darwin notify? | **ANSWERED** | Appears next to ComputerUseIPCJSONRPCSocket / ComputerUseIPCXPCSession. Sibling name com.openai.sky.computer-use-json-rpc-socket is the dispatch queue. The -readiness suffix is the corresponding readiness signal (dispatch queue name pattern, not a documented notify_post name we could match). Who waits besides the service: not visible in JS. | service strings json-rpc-socket and json-rpc-socket-readiness |
| 07-native-app | 6 | Why no NSAccessibilityUsageDescription / NSScreenCaptureUsageDescription? | **ANSWERED** | CUA Info.plist has NSAppleEventsUsageDescription (Messages copy) and NSContactsUsageDescription only. Prompts use in-app 'ChatGPT Computer Use' window plus System Settings Privacy_Accessibility / Privacy_ScreenCapture. Whether AXIsProcessTrustedWithOptions still shows a system sheet with that app name is not testable from plist alone; in-app UI is the intended UX (pending-permissions copy names that window). | `copies/CUAService.Info.plist.xml`; permission strings in 07 FINDINGS |
| 07-native-app | 7 | Input Monitoring vs Accessibility for EventTap | **UNANSWERED** | EventTap/CGEventTap/clickEventTap/keyboardEventTap strings in AccessibilitySupport. No Privacy_ListenEvent / NSIsAlwaysAllowed / ListenEvent usage string. Whether tapping is covered by Accessibility or unused at runtime is compiled. No TCC.db read (SIP). | 07 FINDINGS EventTap strings; Info.plist |
| 07-native-app | 8 | Who holds TCC identity — service, ChatGPT, or both? | **ANSWERED** | Clicker is SkyComputerUseService bundle com.openai.sky.CUAService (separate from com.openai.codex). Permission window copy is ChatGPT Computer Use (the service app’s display name). AXIsProcessTrusted is expected in the service process; ChatGPT is parent/PIP. Both may appear in TCC.db as clients; db unreadable here. JS does not proxy AX. | Info.plist CFBundleIdentifier; live pid 3802; 07 FINDINGS |
| 07-native-app | 9 | NSAppleEventsUsageDescription is Messages copy — dead, SlimCore, or real AE? | **ANSWERED** | Plist string is Messages/recipients copy. Service has MessagesAppleEventsPermission / CalendarAppleEventsPermission and ScriptingBridge. SkyComputerUseClient runs Messages/Calendar MCP in the same helper. Real AE path for those MCPs; not used by JS click/type. Shared SlimCore resources also ship in the app. | CUAService.Info.plist.xml; MessagesAppleEventsPermission strings |
| 07-native-app | 10 | Audio recording TCC: Microphone vs AudioCapture? | **UNANSWERED** | JS start_audio_recording gated by SKY_ENABLE_AUDIO=1; elicitation 'Allow Computer Use to record computer audio?'. Service links AVFoundation. No NSMicrophoneUsageDescription / NSAudioCaptureUsageDescription in CUA plist. Which TCC service is requested is compiled. Live audio env unset. | computer-use-policy.js requestComputerAudioApproval; Info.plist; AVFoundation |
| 07-native-app | 11 | SERVICE_AGENT_LABEL unused? | **ANSWERED** | BUILD.bazel define SERVICE_AGENT_LABEL=com.openai.sky.CUAService.agent. No LaunchAgent, no SMAppService strings, live process is unmanaged child of ChatGPT (com.apple.xpc.launchd.unmanaged.SkyComputerUseS.3802). Unused in this install. | `copies/CUAService.BUILD.bazel`; 07 FINDINGS launchd |
| 07-native-app | 12 | If ChatGPT quits, does the service exit? | **ANSWERED** | Live ppid is ChatGPT.app. Unmanaged XPC child, no keep-alive LaunchAgent found. Expected to exit with parent; no keep-alive we could see. Not observed by quitting ChatGPT in this pass. | ps ppid 3579; no SMAppService |
| 07-native-app | 13 | normal vs relaxed builds / ComputerUseAllowForbiddenTargets | **ANSWERED** | Same as 02-26. AllowForbiddenTargets string exists in this binary (override flag). Who ships relaxed: package.json lists both; ChatGPT nested app is unlabeled single copy. Relaxed likely enables ComputerUseAllowForbiddenTargets. | package.json publishConfig; ComputerUseAllowForbiddenTargets |
| 07-native-app | 14 | Who upgrades installed auth plugin 1000366 → bundled 1000968? | **UNANSWERED** | Installer tool: install\|uninstall\|status RESOURCE_DIR; copies to /Library/Security/SecurityAgentPlugins/… and patches authorizationdb. Sparkle feed on the nested app (cua/alpha/appcast.xml) could update the app (and bundled plugin) independently of ChatGPT. Who invokes the installer on first-run (ChatGPT vs Sparkle vs manual) is not in JS. Installed copy is older — upgrade did not run here. | InstallerTool usage string; Info.plist SUFeedURL; version diff |
| 07-native-app | 15 | system.login.screensaver evaluate-mechanisms insertion / mechanism name | **ANSWERED** | Installer patches system.login.screensaver evaluate-mechanisms. Mechanism class CodexComputerUseMechanism. Extra right CodexComputerUseAuthorizationPlugin:allow. authorizationdb read was unreadable here so live insertion not confirmed on this Mac, but the tool’s contract is those names (not the bundle id as mechanism). | 07 FINDINGS installer; plugin binary CodexComputerUseMechanism |
| 07-native-app | 16 | Lock-screen socket 0666 — is signing check always on? Wire protocol? | **ANSWERED** | Plugin always copies peer audit token, signing id com.openai.sky.CUAService, team 2DC432GLL2; mismatch string 'Login authorization socket peer identity mismatch'. Check is the auth path, not optional in strings. Response log 'Login authorization socket response=%s allowed=%@' — not proven to be a single line allowed=true/false on the wire, but that format is the debug string. Socket mode 0666 with identity in-process. | installed plugin strings; live sock mode 0666 |
| 07-native-app | 17 | Auto-unlock: submit password via plugin or skip screensaver UI? (do not test) | **ANSWERED** | Both pieces exist: LockScreenAutoUnlockCoordinator; errors 'automatic unlock could not unlock it' / 'paused because physical input was detected'; plugin Allow/Deny to SecurityAgent; SystemLockScreenAXInteractor / UserPasswordTextField. Plugin returns Allow/Deny to the screensaver evaluate-mechanisms — that is skip/allow unlock, not JS submitting the login password over MCP. Password-field AX types exist (do not test). ComputerUseIPCRequestExemptFromLockScreenAutoUnlock exists. | service/plugin strings; do-not-test |
| 07-native-app | 18 | Guardian has no entitlements — how AX/ScreenCapture at lock screen? | **UNANSWERED** | Guardian binary has no entitlements blob; spawned by the service via Mach rendezvous; contains AX/ScreenCaptureKit/overlay types (SystemLockScreenAXInteractor). TCC inheritance vs loginwindow special case vs separate client cannot be read (TCC.db SIP). Not answered from entitlements alone. | codesign entitlements none; guardian strings |
| 07-native-app | 19 | Where does org denied vs safety forbidden live? | **ANSWERED** | Native ComputerUsePolicyProviding / CodexAppServerComputerUsePolicyProvider returns decision allowed\|denied\|forbidden plus risk. JS maps denied→org message, forbidden→safety message. Statsig present; ComputerUseAllowForbiddenTargets override flag; hardcoded bundle-id samples in the binary (not a complete list). MDM vs Statsig split is compiled. | `computer-use-policy.js`; native decision strings |
| 07-native-app | 20 | URL blocklist — browser windows only or any AX-visible URL? | **ANSWERED** | Types ComputerUseURLBlocklist / isURLBlocked / isWebBrowser. Also EventStreamURLPolicyRecordFilter. Applied when the target is classified as a web browser (isWebBrowser), not only Chromium-via-extension. Any AX-visible URL on a browser app can hit −10015. Exact pattern list not recovered. | native isURLBlocked/isWebBrowser; errors -10015 |
| 07-native-app | 21 | system security process block — which bundle ids? | **ANSWERED** | Message 'Computer use actions are not allowed for system security process:'. Nearby: com.apple.SecurityAgent, LocalAuthentication.UIAgent / RemoteService, WindowManager, controlcenter, notificationcenterui, UserNotificationCenter. loginwindow / systempreferences appear elsewhere (prefs in Privacy_ / bundle list). Not a fully extracted table; those ids are the security-process cluster. | SkyComputerUseService strings around that error |
| 07-native-app | 22 | Does unified-computer-use ever exec SkyComputerUseClient? | **ANSWERED** | No. JS path is @oai/sky → socket. computer-use plugin launcher execs SkyComputerUseClient mcp; that MCP is disabled here. Codex CLI/notify still execs the client for turn-ended. ChatGPT desktop PiP uses sky.node XPC, not the MCP. | launch.mjs; config.toml computer-use enabled=false; notify argv |
| 07-native-app | 23 | Messages/Calendar MCP same TCC identity as click/type? | **ANSWERED** | Same SkyComputerUseClient process and XPC session to the same service (same bundle id). Separate permission gates: MessagesAppleEventsPermission, CalendarAppleEventsPermission, Contacts usage string. Calendar MCP is a not_implemented placeholder. Not the same as Accessibility/Screen Recording, but same code identity. | SkyComputerUseClient MCP servers; permission type names |
| 07-native-app | 24 | Is Skysight enabled in this ChatGPT build? | **ANSWERED** | Compiled into service and guardian (full IPC types + Statsig events). Desktop feature schema has skysight: n.Xl(). No live Skysight MCP process in the 04 dump. Treat as flag-gated / separate MCP, not on in this CUA js session. | native Skysight* types; app.asar features.skysight; no live MCP |
| 07-native-app | 25 | Does nested app self-update via Sparkle independently of ChatGPT.app? | **ANSWERED** | CUA Info.plist SUFeedURL=https://oaisidekickupdates.blob.core.windows.net/mac/cua/alpha/appcast.xml, automatic updates enabled. That is an independent Sparkle feed from ChatGPT.app. Home copy has not drifted from the bundle yet (identical SHA). Independent update is possible and would explain future drift. | `copies/CUAService.Info.plist.xml`; 07 SHA match |
| 07-native-app | 26 | sky.node vs @oai/sky — overlay only? | **ANSWERED** | sky.node: spawnComputerUseService, XPC to CUAService, setComputerUseCursorLocation, RemoteHostedPIPContent. It does not implement click/type (those are the service). It is the Electron PIP cursor overlay + service spawn, not the JS library. | `/Applications/ChatGPT.app/Contents/Resources/native/sky.node` strings |
| 07-native-app | 27 | TCC.db unreadable under SIP | **OUT OF SCOPE** | Noted. Cannot list kTCCServiceAccessibility/ScreenCapture/ListenEvent clients for CUAService vs codex without FDA/tccutil. Not pursued. | 07 QUESTIONS.md item 27 |
| 07-native-app | 28 | x86_64 — Rosetta or arm-only? | **ANSWERED** | All CUA binaries in this tree are arm64-thin (`file` Mach-O 64-bit executable arm64). No x86_64 slice in the nested app. Intel Macs would need another package/slice (not in this ChatGPT.app darwin-arm64 cua_node archive) or fail. Not Rosetta-fat. | `file SkyComputerUseService`; cua_node manifest darwin-arm64 |

## Notes

- Raw websocket: `/Users/dongdong/Desktop/codex-traffic/2026-09-10/0127_WS_backend-api_codex_responses/websocket.jsonl` and `0223_…/websocket.jsonl` (also `/Users/dongdong/Desktop/codex拦截-两轮-raw.json`).
- Agent 04 'Not in scope' bullets (SkyComputerUseClient full MCP list while not running, full kernel Mach-O sources, whether omit_tools_from is honored by code_mode_host) were not numbered and are not rows. Live MCP tools/list was obtained later in `verify/results/mcp-ndjson.json` (10 tools, no paste).
- Agent 06 'Not inspected' bullets were not numbered.
- Do-not-test items (lock-screen auto-unlock password submission) were answered from strings only.

